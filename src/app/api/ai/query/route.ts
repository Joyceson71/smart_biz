/**
 * /api/ai/query — REMOVED (REDUNDANT)
 *
 * This route was a duplicate placeholder of /api/chat which is the
 * canonical, fully-implemented AI streaming chat endpoint.
 * Use /api/chat for all AI interactions.
 */

export {};
